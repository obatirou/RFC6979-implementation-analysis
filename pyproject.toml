[project]
name = "testuv"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "cryptography>=43.0.3",
    "ecdsa>=0.19.0",
    "eth-keys>=0.6.0",
    "ruff>=0.7.3",
]
